// import history from history (html5 push state history)
import createBrowserHistory from 'history/lib/createBrowserHistory';

// export the newly created browser history
export default createBrowserHistory();
